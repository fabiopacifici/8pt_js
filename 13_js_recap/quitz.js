/* 

Sei un assistente specializzato nella generazione di domande sull’argomento delimitato con "## Argomento".

Usa le note fornite di seguito dal docente delimitate con "## Note" per generare delle domande senza risposte per supportare le mie necessità di studio.

Includi domande che incoraggiano il ragionamento logico. E piccoli esercizi pratici da risolvere per testare la comprensione dell'argomento.


## Argomento:
array e metodi di array in JavaScript, ciclo for, manipolazione del dom


## Note:
ho difficoltá nell'utilizzo dei metodi di array in JavaScript. Vorrei approfondire i metodi di array e come posso utilizzarli per risolvere problemi comuni.

## Output:
fornisci le domande come commenti.


*/

// Qual è la differenza tra un array e un oggetto in JavaScript?

// Scrivi un ciclo for che stampa tutti gli elementi di un array chiamato "numeri".

// Quali sono i principali metodi di array in JavaScript che permettono di aggiungere o rimuovere elementi? Fai degli esempi di utilizzo.

// Cosa fa il metodo .map()? Scrivi un esempio pratico di utilizzo per trasformare un array di numeri in un array di stringhe.

// Come funziona il metodo .filter()? Prova a filtrare da un array di numeri solo quelli maggiori di 10.

// Qual è la differenza tra .forEach() e .map()? In quali casi useresti uno invece dell’altro?

// Scrivi una funzione che, dato un array di nomi, restituisce un nuovo array con solo i nomi che iniziano per la lettera "A".

// Come puoi trovare l’indice di un elemento in un array? E come puoi rimuovere quell’elemento?

// Cosa succede se usi il metodo .push() su un array? E se usi .pop()?

// Scrivi un piccolo esercizio: dato un array di numeri, crea un nuovo array che contiene solo i numeri pari.

// Come puoi modificare il contenuto di un elemento HTML usando JavaScript e il DOM?

// Scrivi un esempio di come aggiungere un nuovo elemento <li> a una lista <ul> esistente nel DOM usando JavaScript.

// Qual è la differenza tra innerHTML e textContent nella manipolazione del DOM?

// Come puoi selezionare tutti gli elementi con una certa classe nel DOM? E come puoi modificarne il testo?

// Esercizio pratico: crea una funzione che riceve un array di stringhe e crea una lista <ul> dinamicamente nel DOM, aggiungendo ogni stringa come <li>.

// Come puoi iterare su tutti gli elementi di un array e applicare una funzione a ciascuno di essi?

// Quali problemi potresti incontrare usando un ciclo for classico rispetto ai metodi di array come .forEach()?

// Scrivi una funzione che somma tutti i numeri di un array usando il metodo .reduce().

// Come puoi verificare se una variabile è un array in JavaScript?

// Esercizio: dato un array di oggetti con proprietà "nome" e "età", crea un nuovo array con solo i nomi delle persone maggiorenni.